---
import { siteConfig } from '@config/site';
import avatar from '@assets/avatar.jpg';
import Now from '@components/widget/Now.astro';
import Bookmarks from '@components/widget/Bookmarks.astro';
---

<aside class="sidebar" aria-label="博客侧栏">
  <section class="side-card profile-card">
    <img
      class="profile-card__avatar"
      src={avatar.src}
      alt={`${siteConfig.author} 的头像`}
      width={avatar.width}
      height={avatar.height}
    />
    <p class="side-card__eyebrow">About me</p>
    <h2>{siteConfig.author}</h2>
    <p>{siteConfig.description}</p>
    <div class="profile-card__status">
      <span aria-hidden="true"></span>
      正在虚质空间漂流中……
    </div>
  </section>

  <Now />
  <Bookmarks />
</aside>
