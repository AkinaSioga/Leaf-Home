# AGENT.md

本文件定义 `D:\.codex\leaf` 项目的协作边界与交付标准。任何自动化代理或开发者在开始任务前，都必须先完整阅读本文件，再检查项目当前状态。若本文件在任务期间发生变化，应立即重新阅读。

## 1. 启动协议

每次开始工作时，依次执行：

1. 阅读根目录的 `AGENT.md`。
2. 确认当前工作目录为 `D:\.codex\leaf`。
3. 检查仓库状态、现有文件和未提交改动。
4. 阅读与任务直接相关的配置、源码和文档。
5. 明确本次任务范围、验收条件和潜在风险后再修改。

不得依赖上一次会话记忆代替当前文件与仓库状态。

## 2. 项目目标

本项目是使用 Astro 构建并部署到 GitHub Pages 的静态个人博客。

技术栈边界：

- Astro：路由、布局、内容渲染和静态生成。
- TypeScript：默认语言，用于配置、数据模型和业务逻辑。
- JavaScript：仅用于确有必要的浏览器脚本或兼容场景。
- Svelte：仅用于需要客户端交互的组件，并优先采用 Astro Islands。
- CSS：全局重置、设计令牌、基础排版和少量通用样式。
- Stylus：页面与组件的主要样式预处理方案。
- Markdown：博客文章的默认内容格式。

项目必须保持静态输出，不引入服务器运行时、数据库或需要常驻后端的功能，除非用户明确改变这一目标。

## 3. 工作边界

- 默认仅修改 `D:\.codex\leaf` 内与当前任务直接相关的文件。
- 不覆盖、删除或回退用户已有改动。
- 不执行 `git reset --hard`、强制推送、批量删除等破坏性操作。
- 不提交、推送、创建远程仓库、Pull Request 或部署，除非用户明确要求。
- 不修改 GitHub Pages、DNS、域名、Secrets 或其他外部服务配置，除非用户明确授权。
- 不读取、输出、提交或硬编码密码、令牌、私钥及其他敏感信息。
- 不随意增加依赖。新增依赖前应确认必要性、维护状态和静态构建兼容性。
- 不为了顺手优化而扩大任务范围；发现范围外问题时记录并告知用户。
- 需求存在关键歧义且不同选择会显著改变结果时，应先说明影响并请求确认。

## 4. 实施原则

- 静态优先：能在构建期完成的工作，不放到客户端运行。
- Astro 优先：普通展示组件优先使用 `.astro`。
- 少量水合：Svelte 组件只有在确需交互时才使用 `client:*` 指令。
- TypeScript 优先：新增逻辑默认使用严格类型，避免无说明的 `any`。
- 渐进增强：禁用 JavaScript 后，核心阅读与导航能力仍应可用。
- 路径安全：所有内部链接和静态资源必须兼容 GitHub Pages 的 `base` 路径。
- 内容安全：文章 frontmatter 必须通过内容集合 schema 校验。
- 可访问性：使用语义化 HTML、键盘可操作控件、可见焦点和合理的颜色对比度。
- 响应式：至少验证窄屏手机和桌面宽度。
- 性能克制：避免无必要的客户端包、字体、图片和动画开销。
- 保持聚焦：优先做小而完整、容易审查和回退的改动。

## 5. 文件与代码规范

- 保持现有目录结构和命名风格；若尚未建立，则采用清晰、可预测的结构。
- 源码、配置和文档统一使用 UTF-8。
- 不编辑生成目录或依赖目录，例如 `dist/`、`.astro/`、`node_modules/`。
- 公共类型集中管理，组件局部类型保持就近定义。
- 全局 CSS 只放真正全局的规则；组件样式默认保持局部作用域。
- Stylus 变量、mixins 和设计令牌应集中组织，避免散落的魔法值。
- 注释解释设计原因和约束，不复述显而易见的代码。
- 新功能应同步更新相关 README、示例内容或配置说明。

## 6. 自检要求

完成修改后，必须按风险执行适当检查。项目脚本可用时，至少运行：

```powershell
pnpm astro check
pnpm build
```

还应检查：

1. 查看改动列表和 diff，确认没有夹带无关文件。
2. 确认没有提交 `node_modules/`、`dist/`、缓存、日志或敏感信息。
3. 验证 Astro 内容 schema、TypeScript 和静态构建通过。
4. 验证 GitHub Pages `site`、`base`、内部链接和资源路径。
5. 验证首页、文章页、列表页、标签页和 404 等受影响页面。
6. 验证移动端布局、键盘操作、明暗主题及禁用 JavaScript 时的核心体验。
7. 若无法运行某项检查，明确说明原因，不得声称已通过。

只修改文档时，可以跳过完整构建，但仍须检查 diff、格式、链接和指令一致性。

## 7. 交付协议

交付说明必须包含：

- 实际完成的内容。
- 关键文件及设计决策。
- 已执行的检查及结果。
- 未执行的检查、遗留风险或需要用户决定的事项。

不得把“文件已修改”描述为“功能已验证”，也不得隐瞒警告、失败或未完成项。

## 8. 指令优先级

发生冲突时，按以下优先级处理：

1. 平台与安全约束。
2. 用户当前明确指令。
3. 本 `AGENT.md`。
4. 项目其他文档和既有代码惯例。

若高优先级指令临时覆盖本文件中的规则，应在交付说明中指出该偏离。

## 9. 协作方式与参考边界

- Codex 默认承担辅助角色，以分析、规划、审查和建议为主。
- 除非用户明确要求写入或实现，否则不得创建、修改或删除项目文件。
- `CuteLeaf/Firefly` 仅作为视觉风格参考；不得照搬其源码、文件结构或素材。若未来确需采用其具体实现，必须先说明来源、许可证和影响，并获得用户确认。
- 将 `AkinaSuki.love` 从旧博客切换到本项目之前，必须先通过临时地址完成验收，并制定可执行的旧站回退方案；未经用户明确授权，不得修改域名、DNS 或线上部署配置。
